// go version >= 1.11, there is no put the code in the GOPATH, go mod was introduce to replace this
// first line of the go module, we can follow this format module {remote}/{username}/xxx , not forces in local development
module Go

go 1.21.4
